## Unreleased

### Changed

- Switched the Katara OAuth2 credential to n8n's dynamic client registration flow by default so users can authorize a freshly registered client instead of supplying client credentials manually.
